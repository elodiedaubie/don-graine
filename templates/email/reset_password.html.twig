<h1>Réinitialisation de mot de passe</h1>

<p>Pour réinititaliser votre mot de passe, veuillez cliquer sur le lien ci-dessous</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>Attention, ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>A bientôt sur Graines en l'air!</p>